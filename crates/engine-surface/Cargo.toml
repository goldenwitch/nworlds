[package]
name = "engine-surface"
version = "0.1.0"
edition = "2021"

[dependencies]
engine-branches = { path = "../engine-branches" }
engine-journal = { path = "../engine-journal" }
engine-observation = { path = "../engine-observation" }
engine-time = { path = "../engine-time" }
serde_json = "1"

[dev-dependencies]
engine-presentation = { path = "../engine-presentation" }
engine-sdk = { path = "../engine-sdk" }
