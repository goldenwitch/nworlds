[package]
name = "nworlds-desktop"
version = "0.1.0"
edition = "2021"

[dependencies]
bytemuck = { version = "1", features = ["derive"] }
caravan-demo = { path = "../caravan-demo" }
caravan-domain = { path = "../caravan-domain" }
engine-sdk = { path = "../engine-sdk" }
nworlds-host = { path = "../nworlds-host" }
pollster = "1"
wgpu = "30"
winit = "0.30"
