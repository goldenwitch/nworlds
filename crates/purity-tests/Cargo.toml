[package]
name = "purity-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
engine-api = { path = "../engine-api" }

[dev-dependencies]
trybuild = "1"
engine-presentation = { path = "../engine-presentation" }
