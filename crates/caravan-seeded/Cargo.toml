[package]
name = "caravan-seeded"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
caravan-domain = { path = "../caravan-domain" }
engine-journal = { path = "../engine-journal" }
engine-time = { path = "../engine-time" }